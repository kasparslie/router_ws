import React, {Component} from 'react';

class Home extends Component {
    render() {
        return (
         <div>
             <p>
             Front-end web development is the practice of converting data to a graphical interface, through the use of HTML, CSS, 
             and JavaScript, so that users can view and interact with that data.
             </p>
         </div>
        );
      }
    }

    export default Home